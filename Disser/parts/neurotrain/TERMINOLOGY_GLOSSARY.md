# TERMINOLOGY GLOSSARY - English to Russian

## Core Terms
- activation maximization → максимизация активации
- spiking neural networks (SNNs) → импульсные нейронные сети
- most exciting images (MEIs) → наиболее возбуждающие изображения
- gradient-free → безградиентный
- Tensor Train decomposition → разложение тензорный поезд
- low-rank → малоранговый
- black-box function → функция черного ящика
- feedforward → прямого распространения
- spike trains → импульсные последовательности
- Leaky Integrate-and-Fire (LIF) → дырявая модель интегрирования и запуска
- membrane potential → мембранный потенциал
- surrogate gradient → суррогатный градиент

## Technical Terms
- ResNet → ResNet (keep as is)
- CIFAR-10 → CIFAR-10 (keep as is)
- generative adversarial network (GAN) → генеративно-состязательная сеть
- variational autoencoder (VAE) → вариационный автокодировщик
- latent space → латентное пространство
- convolutional neural network → сверточная нейронная сеть
- backpropagation → обратное распространение
- hyperparameter → гиперпараметр
- optimizer → оптимизатор
- inference → инференс/вывод
- benchmark → эталонный тест

## Neuroscience Terms
- neuron selectivity → селективность нейронов
- neuronal specialization → нейронная специализация
- visual cortex → зрительная кора
- in vivo → in vivo (keep as is)
- in silico → in silico (keep as is)
- receptive field → рецептивное поле

## Mathematical Terms
- optimization → оптимизация
- convergence → сходимость
- iteration → итерация
- discretization → дискретизация
- decomposition → разложение/декомпозиция
- manifold → многообразие
- embedding → вложение

## Abbreviations (keep as is)
- PCA, SNN, ANN, GAN, VAE, LIF, MEI, AM, TT, CPU, GPU
- MNIST, CIFAR-10, ImageNet
- SGD, CMA-ES, SPSA
- PROTES, TTOpt, MANGO

## Phrases
- explainable AI → объяснимый ИИ
- feature visualization → визуализация признаков
- population coding → популяционное кодирование
- reservoir computing → резервуарные вычисления
- critical learning periods → критические периоды обучения